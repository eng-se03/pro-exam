@* @model IEnumerable<pro_exam.Models.Doctor>
@{
    ViewBag.Title = "Doctor Schedules";
}

<h2>Doctors and Schedules</h2>

@foreach (var doctor in Model)
{
    <div>
        <h3>@doctor.DoctorName</h3>

        <h4>Sunday/Tuesday/Thursday</h4>
        <table class="table">
            <thead>
                <tr>
                    <th>Day</th>
                    <th>Start Time</th>
                    <th>End Time</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var monitoring in doctor.Monitorings)
                {
                    if (monitoring.Schedule.Day == "Sunday" || monitoring.Schedule.Day == "Tuesday" || monitoring.Schedule.Day == "Thursday")
                    {
                        <tr>
                            <td>@monitoring.Schedule.Day</td>
                            <td>@monitoring.Schedule.StartTime</td>
                            <td>@monitoring.Schedule.EndTime</td>
                        </tr>
                    }
                }
            </tbody>
        </table>

        <h4>Monday/Wednesday</h4>
        <table class="table">
            <thead>
                <tr>
                    <th>Day</th>
                    <th>Start Time</th>
                    <th>End Time</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var monitoring in doctor.Monitorings)
                {
                    if (monitoring.Schedule.Day == "Monday" || monitoring.Schedule.Day == "Wednesday")
                    {
                        <tr>
                            <td>@monitoring.Schedule.Day</td>
                            <td>@monitoring.Schedule.StartTime</td>
                            <td>@monitoring.Schedule.EndTime</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
}
 *@