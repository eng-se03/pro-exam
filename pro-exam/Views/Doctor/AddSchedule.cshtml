@* @model pro_exam.ViewModel.DoctorScheduleViewModel
@using pro_exam.Models
@inject Microsoft.AspNetCore.Mvc.Rendering.IHtmlHelper HtmlHelper

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Schedule</title>

    <!-- Bootstrap CDN -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="~/css/custom.css" rel="stylesheet">
</head>

<body style="background-color:lightblue">

    <div class="container mt-5">
        <h2 class="text-center mb-4">Add Schedule</h2>
        <form asp-action="AddScheduleToDB" method="post" class="shadow p-4 rounded" style="background-color: #f8f9fa;" id="scheduleForm">
            <div class="form-row">
                <div class="form-group col-md-6">
                    <label for="DoctorId">Select Doctor</label>
                    <select class="form-control" asp-for="DoctorId" required>
                        <option value="">Select a Doctor</option>
                        @foreach (var doctor in ViewBag.Doctors as List<Doctor>)
                        {
                            <option value="@doctor.Id">@doctor.DoctorName</option>
                        }
                    </select>
                </div>

                <div class="form-group col-md-6">
                    <label for="Day">Day</label>
                    <select class="form-control" asp-for="Day" required>
                        <option value="">Select Day</option>
                        <option>Sunday</option>
                        <option>Monday</option>
                        <option>Tuesday</option>
                        <option>Wednesday</option>
                        <option>Thursday</option>
                    </select>
                </div>
            </div>

            <div class="form-row">
                <div class="form-group col-md-6">
                    <label for="StartTime">Start Time</label>
                    <input type="time" class="form-control" asp-for="StartTime" required>
                </div>

                <div class="form-group col-md-6">
                    <label for="EndTime">End Time</label>
                    <input type="time" class="form-control" asp-for="EndTime" required>
                </div>
            </div>

            <div class="form-row">
                <div class="col text-center mt-3">
                    <input type="submit" class="btn btn-primary px-5" value="Add Schedule" id="submitButton">
                </div>
            </div>
        </form>
    </div>

    <!-- Bootstrap CDN JavaScript -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

    <!-- JavaScript Animation Effects -->
    <script>
        // تأثيرات الجافا سكريبت يمكن إضافتها هنا إذا كنت ترغب في الحفاظ على التأثيرات من الكود الأصلي
    </script>
</body>
</html>
 *@